export const general = "general";

export const home = "home";

export const tools = `tools`;

export const qrCodeGenerator = `${tools}.qrCodeGenerator`;

export const qrCodeUrl = `${qrCodeGenerator}.url`;

export const qrCodeText = `${qrCodeGenerator}.text`;

export const qrCodePhone = `${qrCodeGenerator}.phone`;

export const clock = `${tools}.clock`;

export const timer = `${tools}.timer`;

export const utilities = `${tools}.utilities`;

export const passwordGenerator = `${tools}.passwordGenerator`;

export const textToHandwriting = `${tools}.textToHandwriting`;

export const text = `${tools}.text`;

export const textCompare = `${tools}.textCompare`;

export const json = `${tools}.json`;

export const jsonEditor = `${tools}.jsonEditor`;

export const image = `${tools}.image`;

export const imageMerge = `${tools}.imageMerge`;

export const characterCounter = `${tools}.characterCounter`;

export const wordCounter = `${tools}.wordCounter`;

export const instagramPostGenerator = `${tools}.instagramPostGenerator`;